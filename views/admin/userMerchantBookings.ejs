<% locals.hideCatNav = true %>
<%- include('../partials/header') %>

<%
  // Formatting helpers for booking dates, times and money values.
  const money = value => 'S$' + Number(value || 0).toFixed(2);
  const formatDate = value => value ? new Date(value).toLocaleDateString('en-SG', { day: 'numeric', month: 'short', year: 'numeric' }) : '-';
  const formatTime = value => value ? String(value).slice(0, 5) : '-';
%>

<!-- Admin view of all bookings received by one merchant. -->
<main class="container py-5">
  <div class="d-flex justify-content-between align-items-start flex-wrap gap-3 mb-4">
    <div>
      <p class="section-label"><%= t('admin.merchantBookings.eyebrow') %></p>
      <h2 style="font-weight:800;letter-spacing:-.03em;color:var(--ink);margin-bottom:.25rem;"><%= merchant.merchant_name %></h2>
      <p style="color:var(--muted);margin:0;"><%= merchant.owner_name %> · <%= t('admin.verification.' + merchant.verification_status, {}, merchant.verification_status) %> · <%= merchant.is_active ? t('admin.accountStatus.active') : t('admin.accountStatus.disabled') %></p>
    </div>
    <a href="/admin/user-management/merchants" class="btn btn-outline-secondary btn-pill">
      <i class="bi bi-arrow-left me-1"></i><%= t('admin.userManagement.merchantAccounts') %>
    </a>
  </div>

  <!-- Booking table joins customer, service and payment details for audit use. -->
  <div class="card border-0 overflow-hidden" style="box-shadow:var(--shadow);border-radius:var(--r-xl);">
    <div class="table-responsive">
      <table class="table table-hover align-middle mb-0">
        <thead>
          <tr>
            <th><%= t('admin.dashboard.logs.colId') %></th>
            <th><%= t('admin.table.customer') %></th>
            <th><%= t('common.service') %></th>
            <th><%= t('admin.table.dateTime') %></th>
            <th><%= t('admin.table.source') %></th>
            <th><%= t('admin.table.bookingStatus') %></th>
            <th><%= t('admin.table.payment') %></th>
            <th><%= t('common.total') %></th>
          </tr>
        </thead>
        <tbody>
          <% if (!bookings.length) { %>
            <tr><td colspan="8" class="text-center py-5 text-muted"><%= t('admin.merchantBookings.empty') %></td></tr>
          <% } %>
          <% bookings.forEach(booking => { %>
          <tr>
            <td class="mono">#<%= booking.booking_id %></td>
            <td>
              <strong><%= booking.customer_name || t('admin.table.guestCustomer') %></strong>
              <div class="small text-muted"><%= booking.customer_email || booking.customer_phone || '-' %></div>
            </td>
            <td><%= booking.service_name %></td>
            <td><%= formatDate(booking.booking_date) %> <span class="text-muted"><%= formatTime(booking.booking_time) %></span></td>
            <td><span class="badge bg-secondary"><%= booking.source ? t('admin.source.' + booking.source, {}, booking.source) : '-' %></span></td>
            <td><span class="badge rounded-pill" style="background:var(--accent-light);color:var(--accent);"><%= t('admin.bookingStatus.' + booking.status, {}, booking.status) %></span></td>
            <td><%= t('admin.paymentStatus.' + (booking.payment_status || 'unpaid'), {}, booking.payment_status || 'unpaid') %></td>
            <td class="mono"><%= money(booking.paid_amount || booking.total_amount) %></td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </div>
</main>

<%- include('../partials/footer') %>
