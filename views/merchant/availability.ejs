<% locals.hideCatNav = true %>
<%- include('../partials/header') %>

<div class="container py-5">
  <a href="/merchant/dashboard" class="btn btn-outline-secondary btn-pill btn-sm mb-4">
    <i class="bi bi-arrow-left me-1"></i>Dashboard
  </a>

  <div class="d-flex justify-content-between align-items-end mb-4 flex-wrap gap-3">
    <div>
      <p class="section-label">Schedule</p>
      <h2
        style="
          font-weight: 800;
          letter-spacing: -.03em;
          color: var(--ink);
          margin-bottom: .25rem;
        "
      >
        Merchant Availability
      </h2>
      <p style="color: var(--muted); margin-bottom: 0;">
        Set weekly operating hours for customer bookings.
      </p>
    </div>

    <span
      class="badge"
      style="background: var(--accent-light); color: var(--accent);"
    >
      <%= availability.filter(a => a.is_active).length %> active day<%=
      availability.filter(a => a.is_active).length !== 1 ? 's' : '' %>
    </span>
  </div>

  <div class="row g-4">
    <!-- Form -->
    <div class="col-lg-5">
      <div
        class="card border-0"
        style="box-shadow: var(--shadow); border-radius: var(--r-xl);"
      >
        <div class="card-body p-4">
          <h5 style="font-weight: 800; margin-bottom: 1.25rem;">
            <i
              class="bi bi-clock me-2"
              style="color: var(--accent);"
            ></i>
            Add / Update Hours
          </h5>

          <form action="/merchant/availability/save" method="POST">
            <div class="mb-3">
              <label class="form-label">Day</label>
              <select
                name="day_of_week"
                class="form-select rounded-xl"
                required
              >
                <% ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday',
                'Saturday', 'Sunday'].forEach(day => { %>
                  <option value="<%= day %>"><%= day %></option>
                <% }) %>
              </select>
            </div>

            <div class="row g-3 mb-3">
              <div class="col-6">
                <label class="form-label">Start Time</label>
                <input
                  type="time"
                  name="start_time"
                  class="form-control rounded-xl"
                  required
                >
              </div>

              <div class="col-6">
                <label class="form-label">End Time</label>
                <input
                  type="time"
                  name="end_time"
                  class="form-control rounded-xl"
                  required
                >
              </div>
            </div>

            <div class="form-check mb-4">
              <input
                class="form-check-input"
                type="checkbox"
                name="is_active"
                id="is_active"
                checked
              >
              <label class="form-check-label" for="is_active">
                Available for booking
              </label>
            </div>

            <button class="btn btn-primary btn-pill w-100">
              <i class="bi bi-save me-1"></i>Save Availability
            </button>
          </form>
        </div>
      </div>
    </div>

    <!-- Table -->
    <div class="col-lg-7">
      <% if (availability.length === 0) { %>
        <div
          class="card border-0 text-center py-5"
          style="box-shadow: var(--shadow); border-radius: var(--r-xl);"
        >
          <div class="icon-circle-lg mx-auto mb-3">
            <i
              class="bi bi-clock"
              style="font-size: 2rem; color: var(--accent);"
            ></i>
          </div>

          <h6
            style="
              font-weight: 700;
              color: var(--ink);
              margin-bottom: .25rem;
            "
          >
            No availability set yet
          </h6>

          <p
            style="
              color: var(--muted);
              font-size: .875rem;
              margin-bottom: 0;
            "
          >
            Add operating hours so customers know when they can book.
          </p>
        </div>
      <% } else { %>
        <div
          class="card border-0 overflow-hidden"
          style="box-shadow: var(--shadow); border-radius: var(--r-xl);"
        >
          <div class="table-responsive">
            <table
              class="table table-hover align-middle mb-0"
              style="font-size: .8125rem;"
            >
              <thead>
                <tr>
                  <th>Day</th>
                  <th>Start</th>
                  <th>End</th>
                  <th>Status</th>
                  <th style="width: 100px;">Action</th>
                </tr>
              </thead>

              <tbody>
                <% availability.forEach(a => { %>
                  <tr>
                    <td>
                      <strong><%= a.day_of_week %></strong>
                    </td>

                    <td><%= String(a.start_time).slice(0, 5) %></td>
                    <td><%= String(a.end_time).slice(0, 5) %></td>

                    <td>
                      <% if (a.is_active) { %>
                        <span
                          class="badge"
                          style="
                            background: var(--success-bg);
                            color: var(--success);
                          "
                        >
                          Open
                        </span>
                      <% } else { %>
                        <span class="badge bg-secondary">Closed</span>
                      <% } %>
                    </td>

                    <td>
                      <div class="d-flex flex-column gap-2 align-items-start">
                        <button
                          class="btn btn-sm btn-outline-secondary btn-pill edit-availability-btn"
                          style="
                            font-size: .72rem;
                            padding: .28rem .85rem;
                            min-width: 72px;
                          "
                          data-day="<%= a.day_of_week %>"
                          data-start="<%= String(a.start_time).slice(0, 5) %>"
                          data-end="<%= String(a.end_time).slice(0, 5) %>"
                          data-active="<%= a.is_active %>"
                        >
                          Edit
                        </button>

                        <form
                          action="/merchant/availability/<%= a.availability_id %>/delete"
                          method="POST"
                        >
                          <button
                            class="btn btn-sm btn-outline-danger btn-pill"
                            style="
                              font-size: .72rem;
                              padding: .28rem .85rem;
                              min-width: 72px;
                            "
                          >
                            Delete
                          </button>
                        </form>
                      </div>
                    </td>
                  </tr>
                <% }) %>
              </tbody>
            </table>
          </div>
        </div>
      <% } %>
    </div>
  </div>
</div>

<script>
  const editButtons = document.querySelectorAll('.edit-availability-btn');

  editButtons.forEach((btn) => {
    btn.addEventListener('click', () => {
      document.querySelector('[name="day_of_week"]').value =
        btn.dataset.day;

      document.querySelector('[name="start_time"]').value =
        btn.dataset.start;

      document.querySelector('[name="end_time"]').value =
        btn.dataset.end;

      document.querySelector('[name="is_active"]').checked =
        btn.dataset.active == 1;

      window.scrollTo({
        top: 0,
        behavior: 'smooth'
      });
    });
  });
</script>

<%- include('../partials/footer') %>